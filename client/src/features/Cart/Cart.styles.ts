import styled from "styled-components";

export const CartContainer = styled.div`
    column-gap: 32px;
`;

export const CartHeader = styled.thead`
    text-align: left;
    border-bottom: 1px solid #ccc;
`;

export const CartItem = styled.tr``;
